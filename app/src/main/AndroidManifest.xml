<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Quanlynhahang">

        <activity android:name=".DangKyActivity" />

        <activity
            android:name=".CustomerLauncherActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name_customer"
            android:roundIcon="@mipmap/ic_launcher_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StaffLauncherActivity"
            android:exported="false" />

        <activity android:name=".DangNhapActivity" />

        <activity android:name=".GioHangActivity" />

        <activity android:name=".ChiTietDonHangActivity" />

        <activity android:name=".XacNhanDonHangActivity" />

        <activity android:name=".NhanVienActivity" />

        <activity android:name=".QuanTriActivity" />

        <activity
            android:name=".TrungTamQuanTriActivity"
            android:exported="false" />

        <activity
            android:name=".TrungTamNoiBoActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="false" />

    </application>

</manifest>
